#!/bin/sh

FOLDER=$HOME/.config/wallpapers
SCRIPT=$HOME/.local/bin/walrefresh

CHOICE=$(nsxiv -otb $FOLDER)

case $CHOICE in
        *.*) wal -n -i "$CHOICE" -o "$SCRIPT" ;;
        *) exit 0 ;;
esac
